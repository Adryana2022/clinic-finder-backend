app:
  name: Clinic-Finder
build:
  dockerfile: Dockerfile
deploy:
  image: clinicfinder
  env:
  - name: DB_DIALECT
    value: postgres
  - name: DB_HOST
    value:
  -name: DB_USER
    value:
  -name: DB_PASSWORD
    value:
  - name: DB_DATABASE
    value:
  -name: DB_PORT
    value: AUTO